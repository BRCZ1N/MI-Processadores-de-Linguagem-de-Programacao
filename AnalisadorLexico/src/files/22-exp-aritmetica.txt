const { }

var {}

start ( ) {

	soma = local.valor + global.valor;
	
	local.soma = (5 + a)  ( (10.5 + 6) / 8.0 ); 
	    
	global.soma = local.soma + vetor[10] * matrix[i][j] + ( ( (10 + alfa) / 2) * Max ;
		
	soma = (a + b * max / 2.5 - ++);
		
	soma = aluno.nota[k] + peso / total;	
}